{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"C:\\Users\\john.shenouda\\Desktop\\IA anglais\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: sync changes\" && git push) 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
